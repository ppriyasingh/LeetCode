class Solution {
    public int majorityElement(int[] nums) {
        
        Arrays.sort(nums);
        
        int i=0,res=0, len=nums.length;
        
        
        return nums[len/2];
    }
}